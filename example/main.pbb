{ cwd, ... }:
let
    compile_obj = pb.rule (
        {input, output, ...}:
        {
            command = ["gcc" "-c" "-o" output input];
        }
    );
in
(pb.build {
    output = cwd / "app";
    rule = compile_obj;
})