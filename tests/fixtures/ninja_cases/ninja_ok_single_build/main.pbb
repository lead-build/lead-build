|{ cwd, pb, ... }|
let
    cc_rule = pb.rule (
        |{ input, output }| {
            command = [ "cc", "-c", input, "-o", output ];
            description = "compile";
        }
    );
in
cc_rule {
    input = cwd / "src.c";
    output = cwd / "out.o";
}
