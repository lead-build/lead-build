WHITESPACE = _{ " " | "\t" | "\n" }
entry = _{ SOI ~ expr ~ EOI }

expr = { object | const_int }

object = { "{" ~ (assignment ~ ";")* ~ "}" }

assignment = { ident ~ "=" ~ expr }


ident = { 'a'..'z' ~ ('a'..'z' | '0'..'9' | "_")* }
const_int = { ("+" | "-")? ~ ASCII_DIGIT+ }
